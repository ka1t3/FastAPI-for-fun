# ChemAPI Configuration
# Copy this file and update with your actual values

# API Keys (format: key:name:role,key:name:role)
# In production, use strong, unique keys and store them securely
API_KEYS=chemapi-admin-key-2024:admin:admin,chemapi-user1-key-2024:user1:user,chemapi-user2-key-2024:user2:user

# CORS - Allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate Limiting (requests per minute per API key)
RATE_LIMIT_PER_MINUTE=60

# Database
DATABASE_URL=sqlite:///./chimie.db

# Application
APP_NAME=ChemAPI
APP_VERSION=2.0.0
DEBUG=false